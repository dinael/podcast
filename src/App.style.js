import styled from 'styled-components'

export const AppStyle = {
  Main: styled.main`
    inline-size: min(90ch, 100% - 4rem);
    margin-inline: auto;
  `,

  Link: styled.a `
    color: #61dafb;
  `
}

